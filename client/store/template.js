import axios from 'axios'

/* Action types. */

/* Action creators. */

/* Thunk creators. */

const init = {}

const template = (state = init, action) => {
	switch (action.type) {
		default:
			return state
	}
}

export default template
